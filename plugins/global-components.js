/*
    Components that are used globally in the application can be registered here.
    Once registered, they can be used in any component without needing to import them.

    Usage example:

    import Alert from "~/components/shared/Alert.vue";

    export default defineNuxtPlugin((nuxtApp) => {
        nuxtApp.vueApp.component("Alert", Alert);
    });

    Now, you can use the <Alert> component in any other component without importing it
*/

export default defineNuxtPlugin((nuxtApp) => {});
